    # while !@is_game_over
    #   @game.printBoard
    #   if (@toggle)
    #     while @game.insertMark(player1.sign,gets.chomp.to_i)
    #     end
    #   else
    #     while @game.insertMark(player2.sign,gets.chomp.to_i)
    #   end
    #   (@toggle) ? @game.checkWin(player1.sign) : @game.checkWin(player2.sign)
    # end